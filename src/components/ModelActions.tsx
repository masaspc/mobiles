import { useEffect, useState } from 'react';
export default function ModelActions({id}:{id:string}) { const [favorite,setFavorite]=useState(false); useEffect(()=>setFavorite(JSON.parse(localStorage.getItem('mobiles-favorites')??'[]').includes(id)),[id]); const toggle=()=>{const s=new Set<string>(JSON.parse(localStorage.getItem('mobiles-favorites')??'[]'));favorite?s.delete(id):s.add(id);localStorage.setItem('mobiles-favorites',JSON.stringify([...s]));setFavorite(!favorite)}; const add=()=>{const p=new URLSearchParams(location.search);const ids=(p.get('compare')??'').split(',').filter(Boolean);if(!ids.includes(id)&&ids.length<5)ids.push(id);location.href=`${import.meta.env.BASE_URL}compare?ids=${ids.join(',')}`};return <div className="flex gap-2"><button className="control" onClick={toggle}>{favorite?'★ お気に入り':'☆ お気に入り'}</button><button className="rounded-lg bg-brand px-3 py-2 text-sm font-semibold text-white" onClick={add}>比較に追加</button></div> }
